/*
 * 问题：增加打印栈顶元素但是不出栈、复制栈顶元素、交换栈顶元素、清空栈
 *
 * 本题不再提供答案，只做分析
 *
 * 1、输出栈顶元素
 * 即用pop()得到栈顶元素以后，打印出来再将该值push进去
 *
 * 2、复制栈顶元素
 * 即先pop得到栈顶元素后，将该元素push2次
 *
 * 3、交换栈顶的2个元素
 * 即先pop两次后，在交换push两次
 *
 * 4、清空栈
 * 即将栈指针指导栈底
 *
 */
